#include <string>
#include <vector>

using namespace std;

string solution(int num) {
    string answer = "Even";
    string answer2 = "Odd";
    if(num % 2 ==0)
    {
        return answer;
    }
    else 
    {
    return answer2;
    }
}